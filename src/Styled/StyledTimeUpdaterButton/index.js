import styled from 'styled-components'

const StyledTimeUpdaterButton = styled.button`
  background-color: white;
  margin: 0 10px;
  padding: 5px;
  text-transform: uppercase;

  &:hover {
    cursor: pointer;
  }
`

export default StyledTimeUpdaterButton
