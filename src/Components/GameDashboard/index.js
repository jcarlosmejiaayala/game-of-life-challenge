import React from 'react'

import StyledGameDashboard from 'Styled/StyledGameDashboard'

const GameDashboard = ({ children }) => (
  <StyledGameDashboard>{children}</StyledGameDashboard>
)

export default GameDashboard
